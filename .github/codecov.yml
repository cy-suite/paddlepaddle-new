codecov:
  notify:
    wait_for_ci: false
  token: fce1f75d-553c-4ab1-a559-6f23efbcdcf4

coverage:
  status:
    project:
      default:
        target: 58% # overall project Coverage
        threshold: 1% # Allow the coverage to drop by 1%, and posting a success status.
    patch:
      default:
        target: 90% # lines adjusted  Coverage < 80%  CI will fail
